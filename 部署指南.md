# 部署指南

> 以下任选一个平台。推荐 Render（最简单）或 Fly.io（免费且持久存储）。

---

## 方案一：Render.com（最简单，免费）

1. 把项目上传到 GitHub 仓库
   ```bash
   cd note-site
   git init
   git add .
   git commit -m "init"
   gh repo create note-site --public --source=. --push
   ```

2. 打开 [render.com](https://render.com)，注册并登录

3. 点击 **New +** → **Web Service** → 连接你的 GitHub 仓库

4. 配置：
   | 字段 | 值 |
   |------|-----|
   | Build Command | `npm install` |
   | Start Command | `node server.js` |

5. 在 **Environment Variables** 中添加：
   ```
   UPLOAD_SECRET = 你的秘密密钥
   SITE_TITLE   = 我的学习笔记
   ```

6. 点击 **Create Web Service**，等待部署完成

7. 你会获得一个 `https://xxx.onrender.com` 的免费域名

> ⚠️ 注意：免费层文件系统在每次重新部署后会重置，笔记会丢失。
> 如果需要持久存储，用方案二，或升级 Render 到付费层（$7/月起）。

---

## 方案二：Fly.io（免费，有持久存储）

1. 安装 Fly CLI：https://fly.io/docs/hands-on/install-flyctl/

2. 登录并创建应用：
   ```bash
   fly auth signup    # 或 fly auth login
   cd note-site
   fly launch
   ```

3. Fly 会自动检测 Node.js 项目。部署前，创建持久卷：
   ```bash
   fly volumes create notes_data --size 1 --region hkg
   ```

4. 编辑生成的 `fly.toml`，添加挂载：
   ```toml
   [mounts]
   source = "notes_data"
   destination = "/app/data"
   ```

5. 设置环境变量：
   ```bash
   fly secrets set UPLOAD_SECRET=你的秘密密钥 SITE_TITLE=我的学习笔记
   ```

6. 部署：
   ```bash
   fly deploy
   ```

> ✅ Fly.io 免费层包含 3 个共享 VM + 3GB 持久存储，笔记不会丢失。

---

## 方案三：本地运行 + 内网穿透

如果不想部署到云服务，可以在自己电脑上跑，用内网穿透公开：

1. 本地启动：
   ```bash
   export UPLOAD_SECRET=你的密钥
   node server.js
   ```

2. 用 ngrok 或 Cloudflare Tunnel 暴露端口：
   ```bash
   # ngrok
   ngrok http 3000
   
   # cloudflared
   cloudflared tunnel --url http://localhost:3000
   ```

3. 会得到一个公网可访问的临时域名

---

## 部署后验证

部署完成后，依次检查：

1. 首页是否正常显示：`https://你的域名/`
2. 示例笔记能否打开：`https://你的域名/note/hello-world`
3. 上传页面是否存在：`https://你的域名/upload-你的密钥`
4. 尝试创建一篇笔记，确认保存正常
